# ASSUMES THAT STRONGSWAN HAS BEEN INSTALLED.
# First export the variable. Also enter the location of MEDDLE_ROOT and the names of the certificates
MEDDLE_CONFIG=MEDDLE_ROOT_FILLER/meddle.config
set -a 
source ${MEDDLE_CONFIG} # This ensures that MEDDLE_ROOT and other variables are available


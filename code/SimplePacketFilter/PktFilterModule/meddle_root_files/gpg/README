This folder has the gpg key to encrypt the files. The key to decrypt the files is not in this tree. Please contact ashwin or dave to get access to this key or replace this key


Key creation steps
1. Create a new folder
2. gpg --homedir=${PWD}
3. gpg --homedir=${PWD} --gen-key
4. gpg --homedir=${PWD} --output Meddle.key --export "NAME ENTERED WHILE CREATING KEY"
5. rename secring.gpg as Meddle.secret
6. remove *.gpg
7. gpg --homedir=${PWD}
8. echo "hello" | ${GPG_BIN} --homedir=${GPG_HOME} -o abc.txt --keyring Meddle.key -er "NAME ENTERED" --trust-model always
9. gpg --homedir=${PWD} --no-default-keyring --secret-keyring Meddle.secret --keyring Meddle.key -o abc --passphrase "PASSPHRASE" --decrypt abc.txt
